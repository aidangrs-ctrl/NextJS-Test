version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - ./patch-next.sh
        - env
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'