version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - ./patch-next.sh
        - aws sts get-caller-identity
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'